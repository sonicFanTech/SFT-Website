Place the 8bitoperator JVE font files here if you want the website to bundle the font locally.

Expected filenames used by the CSS:
 - 8bitoperator_jve.ttf
 - 8bitoperator_jve.woff2

The site also tries local("8bitoperator JVE") first, so it may still work if the font is installed on the system.
